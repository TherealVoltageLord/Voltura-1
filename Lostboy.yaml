services:
  - type: web
    name: voltura
    runtime: node
    buildCommand: npm install
    startCommand: node app.js
    env: node
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
